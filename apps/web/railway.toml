[build]
builder = "dockerfile"
dockerfilePath = "apps/web/Dockerfile"

[deploy]
startCommand = "sh -c 'node scripts/migrate.js && node server.js'"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
