# Test-only dependencies for the worker. Install with:
#   pip install -r apps/worker/requirements-dev.txt
-r requirements.txt

# Hermetic in-memory Redis so the registry tests need no live server.
fakeredis==2.36.2
pytest==9.0.3
