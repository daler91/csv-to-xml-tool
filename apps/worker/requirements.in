# Direct runtime dependencies of the worker. Human-edited; the Docker image and
# CI install the hash-locked requirements.txt compiled from it:
#   uv pip compile apps/worker/requirements.in -o apps/worker/requirements.txt --generate-hashes --universal --python-version 3.10
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
redis==8.1.0
defusedxml==0.7.1
lxml==6.1.3
