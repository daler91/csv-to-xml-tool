# Direct dependencies of the desktop CLI and its tests. Human-edited; the
# installable, hash-locked resolution lives in requirements.txt. After changing
# this file regenerate it with (uv: `pip install uv`):
#   uv pip compile requirements.in -o requirements.txt --generate-hashes --universal --python-version 3.10
pytest==9.1.1
pytest-cov==7.1.0
defusedxml==0.7.1
lxml==6.1.3
